streamlit
transformers>=4.30.0 # Specify a recent version
torch>=2.0 # Specify a compatible torch version
accelerate>=0.20.0 # Add accelerate
sentencepiece # Often needed by tokenizers
protobuf # Sometimes needed by tokenizers/models